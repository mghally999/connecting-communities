node_modules
.DS_Store

# Build output
.next
.next-*
out
tmp
build
dist

# Bundle-analyzer reports
**/analyze/*.html

# Local archives (Browsertrix WACZ captures of foam.org + Playwright deep/mega scrapes)
Foam/
foam-deep/
foam-mega-run/
foam_source/
foam-scrape-2/
foam-scrape-2 copy/
foam-connecting-communities.wacz

# Intermediate JSON dumped by scripts/build-talent-data.py for inspection
tmp_talent_artists.json

# Logs
*.log
npm-debug.log*

# Local frontend archives (85 MB tarballs — keep on disk, not in git)
archives/
Archive.zip

# Env files (Next.js convention — never commit secrets)
.env*.local
.env.local

# IDE
.vscode
.idea
