# /talent — Animation timeline (source-derived)

Generated by `scripts/extract-talent-animations.py`. 
Walks `src/components/talent/`, `src/lib/talent-*.js`, and `src/styles/talent.css`, surfacing every framer-motion `motion.X` element, every GSAP call, every CSS keyframe/transition/animation, and every three.js/R3F reference.

## Phase machine (controls everything)

`TalentExperience.js` owns a `phase` state that gates what's rendered and animated:

```
intro      (t=0)     foam wordmark + TALENT op-art fade in
hero-zoom  (t=0.1)   layoutId='hero-card' enters at 85vw
gallery    (t=2.4)   layoutId morph: hero shrinks to grid
portfolio  (on tap)  Portfolio mounts via layoutId; URL
                     swaps via history.pushState
```

## Per-file animation surface

### `src/components/talent/CategoryChipsHud.js` (1 framer-motion node)

- L93 `<motion.div>` — animate, initial, transition

### `src/components/talent/GalleryGrid.js` (2 framer-motion nodes)

- L89 `<motion.div>` — drag, dragConstraints, dragElastic, dragMomentum, whileTap
- L112 `<motion.button>` — layoutId

### `src/components/talent/Intro.js` (4 framer-motion nodes)

- L50 `<motion.div>` — animate, exit, initial, transition
- L116 `<motion.div>` — animate, initial, layoutId, transition
- L93 `<motion.span>` — transition, variants
- L81 `<motion.p>` — animate, exit, initial, transition, variants

### `src/components/talent/Portfolio.js` (3 framer-motion nodes)

- L18 `<motion.div>` — layoutId
- L46 `<motion.div>` — animate, exit, initial, transition
- L76 `<motion.div>` — layoutId, transition

### `src/components/talent/TalentExperience.js` (3 framer-motion nodes)

- L136 `<motion.div>` — animate, initial, transition
- L165 `<motion.div>` — animate, exit, initial, transition
- L198 `<motion.div>` — animate, exit, initial, transition

## GSAP calls

- `src/components/talent/FoamSidebar.js:35` — `gsap.to(el, {`

## CSS keyframes / transitions

- **1 keyframe block**: introBob
- **11 `transition:` declarations** in talent.css
- **1 `animation:` declarations** in talent.css

## WebGL / three.js footprint

_Zero. This build is pure DOM + framer-motion + GSAP (GSAP only in FoamSidebar). Three.js / R3F were deliberately removed in a prior refactor because framer-motion's layoutId morphs (the hero-zoom-into-gallery shared-element animation) require both endpoints to live in the DOM._